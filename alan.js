const yaricap = process.argv.slice(2)[0]*1
function DaireAlaniHesapla() {
    const daireAlani = Math.PI * (r*r)
    console.log(`Yarıçapı (${yaricap}) olan dairenin alanı: (${daireAlani})'dir.`)
}
DaireAlaniHesapla()
